import React from "react";
import { Button } from '@mui/material';
import Link from "next/link";

function Viewall() {
  return (
    // <Link href={`/dashboard/application/${.name}`}>
    <Button variant="contained">View All</Button>
    // </Link>
  );
}

export default Viewall;
